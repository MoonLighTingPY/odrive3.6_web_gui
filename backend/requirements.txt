Flask==2.3.3
Flask-CORS==4.0.0
flask-sock==0.7.0
# ODrive Python library. Talks to the controller over USB.
# Pinned to the last stable release; not required when running with ODRIVE_MOCK=1.
odrive==0.6.10.post0
